document.addEventListener("DOMContentLoaded", ()=>{  // Sert à chargé et analysé le html
console.log("Load");
})

Ecouteur : permet transmettre une information
Emmetteur : 
Methode apparenté a des fonction

compilé : il est traduit en langage machine pour etre interprété par la machine.
intérprété : language interpréte par le navigateur.
transcompilation : compilé par d'autre language.

Language de bas niveau (se rapproche de la machine) : 
Assembleur 0101001
 - C    compilé
 - C++  compilé
 - typeScript => Angular
 - Python => django
 - React => MS
Langage de haut niveau (s'eloigne de la machine) :
 - Java compilé
 - PHP => Synfony, Laravel
 - Node js => EJS intérprété par le navigateur et transcompilé


concaténation : ajouter à la suite

let : variable
const : inchangeable

Methode : exemple : prompt,querySelector,getElements,push

NaN : Not a number

BOM : Browser Object Model - exemple : prompt
DOM : Document Objet Model

Chaque donnée doit etre spécifques
ex: en html on a input text. Le js on doit verifier que c'est un number.

itération = boucle
________________________________________
Objets 
prototipes

CSS:
first mobile : utilisation de max-width pour le first mobile. 

width: auto

un element inline-block, inline, table ne peuvent pas avoir une width auto.

objet:
methode : est une fonction qui apartient a un objet
fonction : avec()
paramettre : 
propriété : 

concaténation != ajouter

La Fonction
Déclarer une fonction :
    let, const
________________________________________
Concept objet

propriete 
variable

________________________________________
Formulaire

form
propriete de form
get : prendre (recherche)
post : envoyer ()

input
propriete input
type :
name :
placeholder :
requiered : champ obligatoire
aria-requiered : pour les noms voyant (indique au non voyant que le champs est obligatoire)
auto-focus : indique le premier champ à remplir
ne jamais mettre l'attribue value
            <button type="submit">Send</button>
button : eviter d'utiliser cette élèment pour un submit

document.getElementsByName("")[0,1,2,3]


________________________________________
CSS
Schéma 
Sélécteur | Propriété | Valeur
* : sélécteur universel