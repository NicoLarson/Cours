package DST;
interface Comparable
{
        /*
         *       * Retourne un nombre negatif si this < other, positif si this > other, 0 si
         *               * this = other.
         *                       */
        public int compareTo(Comparable other);
        if (this.Comparable < other) {
                return -other;
        }
}
